# MemoryBank (Legacy Pointer)

Canonical backend documentation now lives in `docs/`.

Use:

- `docs/README.md` as the index
- `docs/04-Billing-and-Stripe.md` for Stripe flows
- `docs/06-Environment-Variables.md` for env setup
- `docs/10-Deployment-Railway.md` for deploy guidance
- `docs/99-Backlog.md` for pending items

This folder is kept only for backward compatibility with older references.
